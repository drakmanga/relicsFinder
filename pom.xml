<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.4</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>relics</groupId>
	<artifactId>relicsApi</artifactId>
	<version>${revision}</version>
	<name>reliceApi</name>
	<description>relicsApiWarframe</description>
	<url/>
	<licenses>
		<license>
			<name>MIT</name>
			<url>https://github.com/drakmanga/relicsFinder/blob/main/LICENSE</url>
		</license>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>25</java.version>

		<!--
			THE version of Relic Finder. Declared here and nowhere else.

			It reaches the jar manifest, and from there both the User-Agent the
			application introduces itself to warframe.market with and the
			"you have" figure the update check reports. The Windows installer
			reads it (installer\windows\build.ps1), the Docker image takes
			whatever jar the build produced, and the frontend no longer carries
			a version of its own at all — the three package.json files used to,
			and agreeing with this line was left to luck.

			-Drevision on the command line overrides it, which is how a release
			build stamps the tag; release.yml refuses a tag whose three numbers
			are not the three below, so the override can only ever say what
			this line already says.

			Maven's own mechanism for this, so nothing rewrites the file during
			a build. The -SNAPSHOT suffix is what a working copy is: not a
			release, and correctly ordered BEFORE 0.1.0 by the comparison the
			update check does — so a working copy is told the release is newer.
		-->
		<revision>0.1.0-SNAPSHOT</revision>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>


		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
